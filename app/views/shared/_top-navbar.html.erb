<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
  <a class="navbar-brand"
     href="<%if params[:controller] === 'users'%> /users/dashboard <%else%> / <%end%>">Home
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault"
          aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" style="justify-content: flex-end">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown" aria-haspopup="true"
             aria-expanded="false"><%= @email %></a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="#">Something else here</a>
            <a class="dropdown-item" rel="nofollow" data-method="delete" href="/users/sign_out">Log out</a>
          </div>
        </li>
      <% else %>
        <li class="nav-item <%= 'active' if params[:controller] === 'devise/registrations' %>">
          <a class="nav-link" href="/users/sign_up">Sign up</a>
        </li>
        <li class="nav-item <%= 'active' if params[:controller] === 'devise/sessions' %>">
          <a class="nav-link" href="/users/sign_in">Log in</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
<%=  %>